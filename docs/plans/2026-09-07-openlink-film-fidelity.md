# OpenLink film fidelity revision

## Superseding user correction

User explicitly rejected the outer presentation treatment. Remove external chapter titles, labels, captions, progress bar, rounded frame, shadow and blank margins. The product fills the output: a 1280×720 source workspace framed into 1920×1080. Camera focus is bounded to the native workspace. Brand opening and ending are three seconds each; 54 seconds belong to the continuous product demonstration. No slide/card treatment. This supersedes the earlier shot framing below.

**Goal:** Continue the English Zorker OpenLink 60-second film, correcting the real timeline and session composer before motion polish.

**Architecture:** Snapshot the actual ChatTimeline and its presentation dependencies, preserving the event projection and render tree. Extract ChatComposer's actual PromptInput subtree, removing service callbacks and feeding deterministic props. Keep the source app untouched. Borrow Tibo-Please's restrained glyph relay and camera grammar, not its brand or its input design.

**Tech stack:** Existing Remotion 4.0.521, React, Tailwind; pinned project installation.

## Implementation and acceptance

1. Add source extraction with source and output hashes, explicit adaptation inventory, and local Geist font. Verify generated timeline contains real WorkFlowGroup, TaskGroupItem and TaskExecutionRow; composer contains the original compact/expanded class expressions.
2. Feed timestamped demo events into the real timeline projection. Replace clocks, automatic scroll and service callbacks with frame state; use the actual orb painter with frame time. Test idle, working, worked, edit, resend and result.
3. Replace the hand-drawn AgentWork and homepage-style message composer in Journey. Preserve real hierarchy, gaps and footer placement. No Cloud/Local switch in the session composer.
4. Refine intro with glyph relay and a settled brand hold; use close-to-wide product framing and readable holds. Keep English fixtures and the demo/time-compression disclosure.
5. Typecheck; render representative and boundary frames plus out-of-order repeats. Inspect fonts, wrapping, clipping and the actual two component trees. Render revised MP4 to local storage; inspect metadata. Report full-playback/audio verification separately from still QA.

## Shot list (30 fps, half-open)

| Frames | Purpose and subject | Camera / handoff | Evidence / acceptance |
| --- | --- | --- | --- |
| 0–150 | Zorker OpenLink reveal, idea to project | Masked glyph relay, steady logo; paper reveal | Original Zorker SVG; no reference brand reused |
| 150–395 | Brief typed into real composer | Close input view, return to workspace | Source PromptInput structure; text readable before send |
| 395–750 | Work unfolds | Push toward actual Working group; settle | Event fixtures drive original task hierarchy and orb painter |
| 750–1140 | Result and code review | Pull back to include browser, then hold | Clearly illustrative result; real Git-change card |
| 1140–1470 | Edit original request | Focus original message; edit overlay then resend | Same message id; original warning and source editor JSX |
| 1470–1560 | Updated result | Return wide, stable blue result | No fabricated performance measurement |
| 1560–1800 | Invitation | Brand hold and clean final frame | English CTA; music-free unless separately authorized |

Output requires source/provenance, sampled-frame report and MP4; no publication or source-app business changes authorized.
