#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
release_root=$(CDPATH= cd -- "$script_dir/.." && pwd)
exec "$release_root/runtime/node/bin/node" "$release_root/openlinkctl.mjs" "$@"

